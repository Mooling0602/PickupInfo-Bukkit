# CHANGELOG
Changelogs are stored here. GitHub Action use it to generate new release automatically.

Tags are named by title but remove "v" in string.

## v1.1.0

- feat: Support console `/give` and `/clear` commands via inventory snapshot-diff.
> 支持控制台 `/give` 和 `/clear` 命令，通过背包快照对比显示增减提示。

## v1.0.0

- feat: Display picked up or dropped out items in player's client language.
> 以玩家的客户端语言显示拾取和掉落的物品。
